#ifndef Util_hpp
#define Util_hpp

// Funções de utilidade
/* Obtém o custo da rota, iterando por cada ponto desta */
double getRouteCost(std::vector<int> &s);
/* Obtém a carga da rota, iterando por cada ponto desta */
double getRouteLoad(std::vector<int> &s);

#endif /* Util_hpp */
